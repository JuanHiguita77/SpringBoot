spring:
  application:
    name: lastFilter

  datasource:
    url: jdbc:mysql://localhost:3306/filter
    username: root
    password: juansito 123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

